class Province {
  constructor({ name, homeland, command, gold, icons, connections }) {
    this.name = name,
    this.homeland = homeland,
    this.command = command
    this.gold = gold
    this.icons = icons

    this.connections = connections
  }
}

export default Province